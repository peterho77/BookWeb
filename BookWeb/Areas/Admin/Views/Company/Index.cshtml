@using Book.Models

<partial name="_Notification"/>

<div class="container">
	<div class="row pt-4 pb-3">
		<div class="col-6">
			<h2 class="text-center text-capitalize text-primary">Company List</h2>
		</div>
		<div class="col-6 text-end">
			<a class="btn btn-primary" asp-controller="Company" asp-action="Upsert">Create New Company</a>
		</div>
	</div>
	<table id="companyTable" class="table table-striped table-bordered">
		<thead>
			<tr>
				<th>Name</th>
				<th>Street Address</th>
				<th>City</th>
				<th>Phone Number</th>
				<th></th>
			</tr>
		</thead>
		@* <tbody>
			@foreach(var e in Model)
			{
				<tr>
					<td>@e.Title</td>
					<td>@e.ISBN</td>
					<td>@e.Price</td>
					<td>@e.Author</td>
					<td>@e.category.Name</td>

					<td>
						<div class="btn-group w-100" role="group">
							<a asp-controller="Product" asp-action="Upsert" asp-route-id="@e.Id" class="btn btn-outline-primary mx-2 form-control">Edit</a>
							<a asp-controller="Product" asp-action="Delete" asp-route-id="@e.Id" class="btn btn-danger mx-2 form-control">Delete</a>
						</div>
					</td>
				</tr>
			}
		</tbody> *@
	</table>
</div>

@section Scripts
{
	<script src="~/js/company.js"></script>
}

